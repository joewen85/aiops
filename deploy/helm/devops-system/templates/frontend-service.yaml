apiVersion: v1
kind: Service
metadata:
  name: devops-frontend
  labels:
    app: devops-frontend
spec:
  type: ClusterIP
  selector:
    app: devops-frontend
  ports:
    - port: {{ .Values.frontend.servicePort }}
      targetPort: {{ .Values.frontend.servicePort }}
