apiVersion: v1
kind: Service
metadata:
  name: devops-backend
  labels:
    app: devops-backend
spec:
  type: ClusterIP
  selector:
    app: devops-backend
  ports:
    - port: {{ .Values.backend.servicePort }}
      targetPort: {{ .Values.backend.servicePort }}
